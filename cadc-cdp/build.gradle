plugins {
    id 'java-library'
    id 'maven-publish'
    id 'application'
    id 'checkstyle'
}

repositories {
    mavenCentral()
    mavenLocal()
}

apply from: '../opencadc.gradle'

sourceCompatibility = 11

group = 'org.opencadc'

version = '1.4.1'

description = 'OpenCADC CDP client library'
def git_url = 'https://github.com/opencadc/cdp'

mainClassName = 'ca.nrc.cadc.cred.client.Main'

dependencies {
    implementation 'xerces:xercesImpl:[2.12.1,3.0)'
    api 'org.bouncycastle:bcprov-jdk18on:[1.70,2.0)'
    api 'org.bouncycastle:bcpkix-jdk18on:[1.70,2.0)'
    api 'org.opencadc:cadc-util:[1.11,)'
    api 'org.opencadc:cadc-registry:[1.4,)'
    implementation 'javax.servlet:javax.servlet-api:3.1.0'
    
    testImplementation 'junit:junit:[4.13,5.0)'
}

configurations {
    runtime.exclude group: 'org.springframework'
    runtime.exclude group: 'javax.servlet'
}
